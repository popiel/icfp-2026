Read a list of integers and print the same list in reverse order.

Each input is a list: a count `n`, then `n` integers. Print the `n` values in
reverse order.

A test case contains one or more [rounds](/grading#round-based), each of which
carries a single list. Lists may vary in length between rounds. You won't
receive the next list until you finish printing the current one.